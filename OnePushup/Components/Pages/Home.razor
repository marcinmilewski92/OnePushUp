@page "/"
@using OnePushUp.Components.HomeComponents

@if (CurrentUser == null)
{
    <CreateUser OnUserCreated="HandleUserCreated" />
}
else
{
    <DailyPushup CurrentUser="CurrentUser" OnEntryAdded="HandleEntryAdded" />
}
